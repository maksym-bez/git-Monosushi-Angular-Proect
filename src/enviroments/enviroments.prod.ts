import { EnviromentsPipe } from './enviroments';

describe('EnviromentsPipe', () => {
  it('create an instance', () => {
    const pipe = new EnviromentsPipe();
    expect(pipe).toBeTruthy();
  });
});
